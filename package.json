{
    "name": "theramind",
    "version": "1.0.0",
    "description": "TheraMind is a platform for managing therapy sessions and patients.",
    "main": "index.js",
    "scripts": {
        "server": "uvicorn main:app --reload --host 0.0.0.0 --port 8000",
        "build:lambda": "cd backend && bash build_lambda.sh",
        "deploy:lambda": "cd backend && bash deploy_lambda.sh"
    },
    "author": "TheraMind",
    "license": "MIT",
    "dependencies": {
        "fastapi": "^0.105.1"
    }
}